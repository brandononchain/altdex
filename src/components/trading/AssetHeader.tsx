export default function AssetHeader({ title, subtitle, price, change }: { title: string; subtitle: string; price: string; change: string }) { return <div className="flex items-center justify-between border-b border-border-subtle pb-2"><div><div className="text-xl font-semibold">{title}</div><div className="text-xs text-text-muted">{subtitle}</div></div><div className="font-mono text-3xl">{price} <span className="text-xl text-accent-green">{change}</span></div></div>; }
