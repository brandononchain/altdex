export default function CorrelationMatrix({ labels, values }: { labels: string[]; values: number[][] }) { return <table className="w-full text-xs font-mono"><thead><tr><th className="border border-border-subtle p-1"></th>{labels.map(l=><th key={l} className="border border-border-subtle p-1">{l}</th>)}</tr></thead><tbody>{values.map((row,i)=><tr key={i}><th className="border border-border-subtle p-1">{labels[i]}</th>{row.map((v,j)=><td key={j} className="border border-border-subtle p-1 text-center">{v.toFixed(2)}</td>)}</tr>)}</tbody></table>; }
